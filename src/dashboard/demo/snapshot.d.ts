// snapshot.js is written by scripts/build-demo-site.ts from demo/snapshot.json; it exists only in the
// built static demo. Its content is checked by parseSnapshot (./snapshot-source.ts) before any use.
declare const snapshot: unknown;
export default snapshot;
