# Copy to .env (git-ignored) and fill in only the providers you use.
# Never commit a real key. Tollwise reads these from the environment.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
DEEPSEEK_API_KEY=
OPENROUTER_API_KEY=
# Ollama runs locally and needs no key.
OLLAMA_BASE_URL=http://localhost:11434
